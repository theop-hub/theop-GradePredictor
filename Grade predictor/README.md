# theop-Grade predictor
A program running on a website that allows for simple grade prediction, making use of machine learning via scikit.

# How to use
- Download and extract the latest release
- Open terminal and use the ls and cd (directory name) commands to navigate to the grade predictor folder
- run the commands cd app and streamlit run app.py